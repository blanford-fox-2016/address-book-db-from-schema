CREATE TABLE IF NOT EXISTS kontak_group (id INTEGER PRIMARY KEY AUTOINCREMENT,kontak_id INTEGER DEFAULT 0 REFERENCES kontak(id) ON UPDATE CASCADE ON DELETE SET DEFAULT,group_id INTEGER DEFAULT 0 REFERENCES 'group'(id) ON UPDATE CASCADE ON DELETE SET DEFAULT)
